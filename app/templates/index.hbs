<div class="full-width h-100" id="background-image" local-class="gradient-background">
	<div class="container h-100">
		<div class="row justify-content-center h-100">
			<div class="col-md-10 align-self-center">
				{{#each this.model as |announcement index|}}
					{{#unless index}}
						{{terminal announcement}}
					{{/unless}}
				{{/each}}
			</div>
		</div>
	</div>
</div>
{{liquid-outlet}}